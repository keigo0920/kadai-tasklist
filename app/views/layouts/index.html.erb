<h1>メッセージ一覧</h1>

<ul>
  <% @tasklists.each do |tasklist| %>
    <li><%= link_to tasklists.id, task %> : <%= tasklists.content %></li>
        <% end %>
</ul>

<%= link_to '新規メッセージの投稿', new_task_path %>

<h1>メッセージ一覧</h1>